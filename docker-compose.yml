services:
  postgres:
    image: "bitnami/postgresql:latest"
    container_name: liftbumbum-db
    ports:
      - "5432:5432"
    environment:
      POSTGRES_USER: docker
      POSTGRES_PASSWORD: docker
      POSTGRES_DB: liftbumbum_db
